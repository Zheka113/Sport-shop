import React from "react";
//import {useDispatch, useSelector} from "react-redux"
//import { setAuth } from "../toolkitRedux/toolkitSlice";

const Auth = () => {
    return(
        <div>
            auth
        </div>
    )
    // const auth = useSelector(state => state.toolkit.isAuth)
    // const dispatch = useDispatch()
    // return(
    //     <div>
    //         <h3>{auth}</h3>
    //         <button onClick={() => dispatch(setAuth())}>Authoritation</button>
    //     </div>
    // )
}

export default Auth;